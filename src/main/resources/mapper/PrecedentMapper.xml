<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.edu.springboot.domain.precedent.PrecedentMapper">

    <select id="getPrecedentList" resultType="com.edu.springboot.domain.precedent.PrecedentVO">
        SELECT 
            PREC_ID as precId,
            CASE_NO as caseNo,
            COURT,
            TO_CHAR(JUDGE_DATE, 'YYYY-MM-DD') as judgeDate,
            CASE_TYPE as caseType,
            TITLE,
            ONE_LINE as oneLine,
            JUDGMENT,
            AI_SUMMARY as aiSummary,
            KEYWORD_CSV as keywordCsv,
            CREATED_AT as createdAt
        FROM TB_PRECEDENT 
        ORDER BY JUDGE_DATE DESC
    </select>

    <select id="getPrecedentDetail" parameterType="long" resultType="com.edu.springboot.domain.precedent.PrecedentVO">
        SELECT 
            PREC_ID as precId,
            CASE_NO as caseNo,
            COURT,
            TO_CHAR(JUDGE_DATE, 'YYYY-MM-DD') as judgeDate,
            CASE_TYPE as caseType,
            TITLE,
            ONE_LINE as oneLine,
            JUDGMENT,
            AI_SUMMARY as aiSummary,
            KEYWORD_CSV as keywordCsv,
            CREATED_AT as createdAt
        FROM TB_PRECEDENT 
        WHERE PREC_ID = #{precId}
    </select>

</mapper>