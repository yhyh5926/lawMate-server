<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.edu.springboot.domain.member.MemberMapper">

	<resultMap id="MemberResultMap"
		type="com.edu.springboot.domain.member.vo.MemberVO">
		<id property="memberId" column="MEMBER_ID" />
		<result property="loginId" column="LOGIN_ID" />
		<result property="password" column="PASSWORD" />
		<result property="memberType" column="MEMBER_TYPE" />
		<result property="name" column="NAME" />
		<result property="phone" column="PHONE" />
		<result property="email" column="EMAIL" />
		<result property="provider" column="PROVIDER" />
		<result property="phoneVerified" column="PHONE_VERIFIED" />
		<result property="status" column="STATUS" />
		<result property="saveIdYn" column="SAVE_ID_YN" />
		<result property="address" column="ADDRESS" />
		<result property="detailAddress" column="DETAIL_ADDR" />
		<result property="createdAt" column="CREATED_AT" />
		<result property="updatedAt" column="UPDATED_AT" />
		<result property="withdrawnAt" column="WITHDRAWN_AT" />
		<result property="lawyerId" column="LAWYER_ID" />
		<result property="licenseNo" column="LICENSE_NO" />
		<result property="specialty" column="SPECIALTY" />
		<result property="officeName" column="OFFICE_NAME" />
		<result property="approveStatus" column="APPROVE_STATUS" />
	</resultMap>

	<select id="findByLoginId" resultMap="MemberResultMap">
		SELECT m.*, l.LAWYER_ID,
		l.LICENSE_NO, l.SPECIALTY, l.OFFICE_NAME, l.APPROVE_STATUS
		FROM
		TB_MEMBER m
		LEFT OUTER JOIN TB_LAWYER l ON m.MEMBER_ID = l.MEMBER_ID
		WHERE m.LOGIN_ID = #{loginId}
	</select>

	<select id="findByEmail" resultMap="MemberResultMap">
		SELECT m.*, l.LAWYER_ID,
		l.LICENSE_NO, l.SPECIALTY, l.OFFICE_NAME, l.APPROVE_STATUS
		FROM
		TB_MEMBER m
		LEFT OUTER JOIN TB_LAWYER l ON m.MEMBER_ID = l.MEMBER_ID
		WHERE m.EMAIL = #{email}
	</select>

	<insert id="insertMember"
		parameterType="com.edu.springboot.domain.member.vo.MemberVO">
		<selectKey keyProperty="memberId" resultType="long"
			order="BEFORE">
			SELECT SEQ_MEMBER.NEXTVAL FROM DUAL
		</selectKey>
		INSERT INTO TB_MEMBER (
		MEMBER_ID, LOGIN_ID, PASSWORD, MEMBER_TYPE,
		NAME, PHONE, EMAIL,
		PROVIDER, PHONE_VERIFIED, STATUS, SAVE_ID_YN,
		ADDRESS, DETAIL_ADDR,
		CREATED_AT, UPDATED_AT
		) VALUES (
		#{memberId},
		#{loginId, jdbcType=VARCHAR}, #{password, jdbcType=VARCHAR},
		#{memberType, jdbcType=VARCHAR}, #{name, jdbcType=VARCHAR}, #{phone,
		jdbcType=VARCHAR},
		#{email, jdbcType=VARCHAR}, #{provider,
		jdbcType=VARCHAR}, #{phoneVerified, jdbcType=VARCHAR},
		#{status,
		jdbcType=VARCHAR}, #{saveIdYn, jdbcType=VARCHAR}, #{address,
		jdbcType=VARCHAR},
		#{detailAddress, jdbcType=VARCHAR}, SYSDATE, SYSDATE
		)
	</insert>

	<update id="updateMember"
		parameterType="com.edu.springboot.domain.member.vo.MemberVO">
		UPDATE TB_MEMBER
		SET NAME = #{name}, PHONE = #{phone},
		EMAIL = #{email},
		ADDRESS = #{address}, DETAIL_ADDR = #{detailAddress},
		UPDATED_AT = SYSDATE
		WHERE LOGIN_ID = #{loginId}
	</update>

	<update id="deleteMember">
		UPDATE TB_MEMBER SET STATUS = 'WITHDRAWN', UPDATED_AT
		= SYSDATE
		WHERE MEMBER_ID = #{memberId}
	</update>

	<update id="withdrawMember">
		UPDATE TB_MEMBER
		SET STATUS = 'WITHDRAWN',
		WITHDRAWN_AT = SYSDATE, UPDATED_AT = SYSDATE
		WHERE LOGIN_ID =
		#{loginId}
	</update>

	<select id="findLoginIdByNameAndPhone" resultType="string">
		SELECT
		LOGIN_ID FROM TB_MEMBER
		WHERE NAME = #{name} AND PHONE = #{phone} AND
		STATUS != 'WITHDRAWN'
	</select>

	<update id="updatePassword">
		UPDATE TB_MEMBER SET PASSWORD = #{newPassword},
		UPDATED_AT = SYSDATE
		WHERE LOGIN_ID = #{loginId}
	</update>

	<select id="selectAllMembers" resultMap="MemberResultMap">
		SELECT m.*, l.LAWYER_ID
		FROM TB_MEMBER m
		LEFT OUTER JOIN TB_LAWYER l ON m.MEMBER_ID =
		l.MEMBER_ID
		ORDER BY m.CREATED_AT DESC
	</select>

	<select id="findMembersByType" resultMap="MemberResultMap">
		SELECT m.*, l.LAWYER_ID
		FROM TB_MEMBER m
		LEFT OUTER JOIN TB_LAWYER l ON m.MEMBER_ID =
		l.MEMBER_ID
		WHERE m.MEMBER_TYPE = #{memberType}
		ORDER BY m.CREATED_AT
		DESC
	</select>

	<select id="findWithdrawnMembersForAnonymization"
		resultMap="MemberResultMap">
		SELECT * FROM TB_MEMBER
		WHERE STATUS = 'WITHDRAWN' AND
		WITHDRAWN_AT &lt;= SYSDATE - 30
	</select>

	<update id="anonymizeMember"
		parameterType="com.edu.springboot.domain.member.vo.MemberVO">
		UPDATE TB_MEMBER
		SET LOGIN_ID = #{loginId}, EMAIL =
		#{email}, NAME = '알수없음', PHONE = '000-0000-0000',
		PASSWORD = NULL,
		ADDRESS = NULL, DETAIL_ADDR = NULL, PROVIDER = NULL,
		STATUS =
		'ANONYMIZED', UPDATED_AT = SYSDATE
		WHERE MEMBER_ID = #{memberId}
	</update>

	<select id="findMyPosts" resultType="map">
		<choose>
			<when test="type == 'question'">
				SELECT
				q.QUESTION_ID as "id",
				q.TITLE as "title",
				TO_CHAR(q.CREATED_AT, 'YYYY-MM-DD') as "date",
				(SELECT COUNT(*) FROM
				TB_ANSWER WHERE QUESTION_ID = q.QUESTION_ID) as "commentCount",
				CASE
				WHEN q.STATUS = 'CLOSED' THEN '답변완료' ELSE '대기중' END as "status"
				FROM
				TB_QUESTION q
				WHERE q.MEMBER_ID = #{memberId}
				ORDER BY q.QUESTION_ID
				DESC
			</when>

			<when test="type == 'mockTrial'">
				SELECT
				p.POLL_ID as "id",
				p.TITLE as "title",
				TO_CHAR(p.CREATED_AT, 'YYYY-MM-DD') as "date",
				0 as "commentCount",
				CASE WHEN p.STATUS = 'CLOSED' THEN '투표종료' ELSE '진행중' END as "status"
				FROM TB_POLL p
				WHERE p.MEMBER_ID = #{memberId}
				ORDER BY p.POLL_ID DESC
			</when>

			<otherwise>
				SELECT
				p.POST_ID as "id",
				p.TITLE as "title",
				TO_CHAR(p.CREATED_AT, 'YYYY-MM-DD') as "date",
				(SELECT COUNT(*) FROM
				TB_REPLY r WHERE r.ref_post_id = p.post_id) as "commentCount",
				'작성완료' as "status"
				FROM TB_POST p
				WHERE p.MEMBER_ID = #{memberId}
				AND
				(p.CASE_TYPE != 'MOCK' OR p.CASE_TYPE IS NULL)
				AND (p.STATUS IS NULL
				OR p.STATUS != 'DELETED')
				ORDER BY p.POST_ID DESC
			</otherwise>
		</choose>
	</select>

</mapper>